### Core ###
NODE_ENV=production          # production / development
PORT=3001                    # cổng chạy API
TRANSPORT_PORT=8080          # (nếu app cần)

### Auth ###
JWT_SECRET_KEY=rxPhglGJWPlOW596
JWT_EXPIRATION_TIME=3600     # giây

### Database – master (WRITE) ###
DATABASE_DIALECT=mysql
DATABASE_HOST=127.0.0.1
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=Omi@1234567
DATABASE_NAME=dm_tool_test

### Database – slave (READ) ###
DB_SLAVES_HOST=127.0.0.1
DB_SLAVES_PORT=3306
DB_SLAVES_USERNAME=root
DB_SLAVES_PASSWORD=
DB_SLAVES_DATABASE=dm_tool_test

### Redis cache ###
REDIS_HOST=127.0.0.1         # <‑‑ CHỖ THIẾU
REDIS_PORT=6379
REDIS_PASS=              # để trống nếu không đặt mật khẩu
CACHE_TTL=60                 # (giây) mặc định của CacheModule

### NATS (nếu bật) ###
NATS_ENABLED=false           # true/false
NATS_HOST=localhost
NATS_PORT=4222

### Logging / Swagger ###
ENABLE_ORM_LOGS=true
ENABLE_DOCUMENTATION=true
FALLBACK_LANGUAGE=en

# Azure AD
TENANT_ID=dummy            # hoặc GUID thật, VD: 72f988bf-86f1-41af-91ab-2d7cd011db47
CLIENT_ID=dummy            # app‑registration clientId
CLIENT_SECRET=dummy        # client secret
METADATA_DISCOVERY=https://login.microsoftonline.com/common/v2.0/.well-known/openid-configuration
AAD_REDIRECT_URI=http://localhost:8030/auth/aad/callback
SCOPE=openid profile offline_access   # <‑‑ THÊM

